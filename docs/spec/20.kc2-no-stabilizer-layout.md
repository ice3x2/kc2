# KC2 No-Stabilizer Split Layout

| Field | Value |
|---|---|
| Document Type | layout_spec |
| Product | KC2 |
| Scope | Physical keyboard layout |
| Status | decided |
| Date | 2026-06-04 |
| Parent Spec | `../spec.md` |
| Related Research | `../research/kc2-plateless-low-profile-stabilizer-removal-2026-06-04.md` |

## 1. Purpose

This document is the KC2 physical layout subdocument for the plateless low-profile no-stabilizer direction.

The design intentionally splits every current `>=2U` key into smaller physical keys. Duplicate legends such as `LShift`/`LShift`, `BSPC`/`BSPC`, `Enter`/`Enter`, and `Space`/`Space` are accepted as the final physical-key naming decision for this layout.

## 2. Policy

- The no-stabilizer layout uses no `>=2U` physical keys.
- `1.75U` remains the largest physical key width in this layout.
- Existing row extents are preserved so the PCB outline strategy can remain close to the current KC2 draft.
- X3 joined use places the right `Y`/`H` protrusion into the left `T`/`G` recess as the default interlocked reference. The default minimum Edge.Cuts clearance between the left and right PCBs is `1.0 mm`.
- The right `Y`/`H` protrusion vertical inner Edge.Cuts face keeps the X3 `3.6 mm` inner-edge margin. Only the protrusion's horizontal ledges are relieved inward by `0.8 mm` to reduce X-axis binding risk.
- The practical interlocked clearance range is `0.8-1.2 mm` for a 3D-printed bottom housing and magnetic alignment; `0 mm` contact is only a coordinate-alignment reference.
- The joined placement clearance is a bottom-housing and alignment datum. It must not reduce PCB inner-edge routing relief, copper-to-edge clearance, or board-edge safety margin.
- Firmware may map duplicate physical legends to identical or separate behaviors; the physical layout intentionally does not force that choice.

## 3. KLE-Style JSON

```json
{
  "left": [
    ["~", "1", "2", "3", "4", "5", "6"],
    [{ "w": 1.5 }, "TAB", "Q", "W", "E", "R", "T"],
    [{ "w": 1.75 }, "Caps", "A", "S", "D", "F", "G"],
    [{ "w": 1.25 }, "LShift", "LShift", "Z", "X", "C", "V", "B"],
    [{ "w": 1.25 }, "Ctrl", { "w": 1.25 }, "Win", { "w": 1.25 }, "Alt", { "w": 1.25 }, "Fn", "Space", { "w": 1.25 }, "Space"]
  ],
  "right": [
    [{ "x": 0.5 }, "7", "8", "9", "0", "-", "=", "BSPC", { "w": 1.25 }, "BSPC", "Del"],
    ["Y", "U", "I", "O", "P", "[", "]", { "w": 1.75 }, "\\", "Home"],
    [{ "x": 0.25 }, "H", "J", "K", "L", ";", "'", "Enter", { "w": 1.5 }, "Enter", "PgUp"],
    [{ "x": 0.75 }, "N", "M", ",", ".", "/", "RShift", "Fn", "Up", "PgDn"],
    [{ "x": 0.75 }, "B", "Space", "Space", "RAlt", "Fn", "RCtrl", "Left", "Down", "Right"]
  ]
}
```

## 4. Key Counts and Row Widths

| Half | Keys | Row widths |
|---|---:|---|
| Left | 32 | `7U`, `6.5U`, `6.75U`, `7.25U`, `7.25U` |
| Right | 45 | `9.75U`, `9.75U`, `9.75U`, `9.75U`, `9.75U` |
| Total | 77 | Existing row extents preserved |

## 5. Split Decisions

| Original key | Original width | Replacement |
|---|---:|---|
| Left `Shift` | `2.25U` | `1.25U LShift` + `1U LShift` |
| Left `Space` | `2.25U` | `1U Space` + `1.25U Space` |
| Right `BSPC` | `2.25U` | `1U BSPC` + `1.25U BSPC` |
| Right `Enter` | `2.5U` | `1U Enter` + `1.5U Enter` |
| Right `RShift` | `2.0U` | `1U RShift` + `1U Fn` |
| Right `Space` | `2.0U` | `1U Space` + `1U Space` |

## 6. Implementation Notes

- The PCB generator must add 6 physical switch positions and 6 diodes compared with the 71-key routed draft.
- The provisional stabilizer marker generation for `key.w_u >= 2.0` should produce no stabilizer markers for this layout.
- Right-side matrix column assignment must be reviewed. A naive row-array edit can shift column semantics and invalidate the current `R_COL7`/`R_COL8` rationale.
- The ZMK matrix and keymap should explicitly distinguish duplicate physical keys with stable physical IDs even when legends match.
- Joined visual/mechanical renders should use interlocked placement with `1.0 mm` minimum Edge.Cuts clearance by default and label the clearance explicitly, because the physical assembly includes a 3D-printed bottom housing and possible magnets.
- The PCB generator should keep the X3 right `Y`/`H` protrusion vertical face at the `3.6 mm` inner-edge margin and apply the `0.8 mm` inward relief only to the protrusion's horizontal ledges while preserving DRC-clean routing and required copper-to-edge clearance.
- PCB regeneration, routing, and DRC must be rerun after implementation.
